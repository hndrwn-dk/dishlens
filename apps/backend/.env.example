# LLM
OPENAI_API_KEY=

# Use ONE of the following for Vision:

# (A) Service Account JSON (preferred on Vercel). Paste the FULL JSON (single line).
GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account", "...": "..."}'

# (B) or API key for REST fallback:
GOOGLE_VISION_API_KEY=

# CORS (origins allowed to call your API)
ALLOWED_ORIGINS=https://dishlens-one.vercel.app,http://localhost:3000